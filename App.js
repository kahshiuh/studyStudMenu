/**
 *
 *
 * "Need to add selected deck"
 * "Need to fix remove deck and id change issues
 * git add .
 * git commit -m "Added selectDeck button for notifs and editDeckName for each indiv. deck"
 * git push -u origin main
 *
 * @format
 * @flow strict-local
 */

import React from 'react';
import Setup from './src/Setup';

const App = () => {
  return <Setup />;
};

export default App;
